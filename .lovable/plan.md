

## المشكلة

عمود "المندوب" موجود فعلاً في الكود (سطر 112 و 129) وبيجيب اسم المندوب من profiles، لكن العمود مخفي على الشاشات الصغيرة بسبب class `hidden sm:table-cell`. وانت شاشتك دلوقتي 360px يعني أصغر من breakpoint الـ `sm` (640px) فالعمود مش ظاهر.

## الحل

تغيير عمود المندوب من `hidden sm:table-cell` إلى ظاهر دايماً (زي عمود العميل والإجمالي).

## التعديل

**ملف**: `src/pages/ClosedOrders.tsx`
- سطر 112: شيل `hidden sm:table-cell` من header المندوب
- سطر 129: شيل `hidden sm:table-cell` من cell المندوب

تعديل بسيط في سطرين فقط.

